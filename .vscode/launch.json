{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Benchmarks",
            "type": "gdb",
            "target": "${workspaceRoot}/app",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText",
            "preLaunchTask": "Build Benchmarks"
        }
    ]
}