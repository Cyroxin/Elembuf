{
    "tasks": [
        {
            "label": "Build Benchmarks",
            "type": "shell",
            "run": false,
            "compiler": "dmd",
            "archType": "x86_64",
            "buildType": "build",
            "configuration": "",
            "command": "dmd -g -ofapp -fPIC ./bench/* ./source/* && rm -rf *.o",
            "dependsOn": ["Install Dlang"],
            "_generated": false,
            "problemMatcher": []
        },
        {
            "label": "Install Dlang",
            "type": "shell",
            "command": "brew install dub && brew install dmd && brew install ldc"
        }
    ]
}